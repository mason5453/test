
git init
git add .
git commit -m "Initial commit"
git remote add origin git@github.com:aiken2/test.git
git push -u origin master 
Mason
123456

git remote set-url origin git@github.com:aiken2/test.git

/home/aiken/Resume/many_items/resume/code/devopt_test1/my-website
# Replace "your_email@example.com" with the email linked to your GitHub account
ssh-keygen -t ed25519 -C "your_email@example.com"
Linux: xclip -sel clip < ~/.ssh/id_ed25519.pub (if xclip is missing, install it first with sudo apt install xclip).
go to git hub paste the key

ssh -T git@github.com

git push --set-upstream origin master

git checkout main
git pull origin main
git merge maser # failed
git merge master --allow-unrelated-histories
(write sth )
ESC
:wq 
: enter mode 
w write 
q quit

